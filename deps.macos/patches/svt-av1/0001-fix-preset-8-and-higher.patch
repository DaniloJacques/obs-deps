diff --git a/Source/Lib/Codec/enc_mode_config.c b/Source/Lib/Codec/enc_mode_config.c
index d86c9a0a..94a1a92e 100644
--- a/Source/Lib/Codec/enc_mode_config.c
+++ b/Source/Lib/Codec/enc_mode_config.c
@@ -1548,17 +1548,17 @@ static uint8_t get_dlf_level_default(PictureControlSet* pcs, EncMode enc_mode, u
             dlf_level = is_not_last_layer ? 3 : 6;
         } else if (enc_mode <= ENC_M7) {
             dlf_level       = is_not_last_layer ? 3 : 6;
-            modulation_mode = 3;
+            modulation_mode = 2;
         } else if (enc_mode <= ENC_M9) {
             dlf_level       = is_not_last_layer ? 6 : 0;
-            modulation_mode = 3;
+            modulation_mode = 2;
         } else if (enc_mode <= ENC_M11) {
             if (pcs->coeff_lvl == HIGH_LVL) {
                 dlf_level = is_base ? 6 : 0;
             } else {
                 dlf_level = is_base ? 6 : is_not_last_layer ? 7 : 0;
             }
-            modulation_mode = 3;
+            modulation_mode = 2;
         } else {
             dlf_level       = 0;
             modulation_mode = 3;
@@ -1568,13 +1568,13 @@ static uint8_t get_dlf_level_default(PictureControlSet* pcs, EncMode enc_mode, u
             dlf_level = 4;
         } else if (enc_mode <= ENC_M7) {
             dlf_level       = 6;
-            modulation_mode = 3;
+            modulation_mode = 2;
         } else if (enc_mode <= ENC_M10) {
             dlf_level       = is_not_last_layer ? 6 : 0;
-            modulation_mode = 3;
+            modulation_mode = 2;
         } else {
             dlf_level       = is_not_last_layer ? 7 : 0;
-            modulation_mode = 3;
+            modulation_mode = 2;
         }
     }
 
@@ -1592,24 +1592,24 @@ static uint8_t get_dlf_level_rtc(PictureControlSet* pcs, EncMode enc_mode, uint8
     if (pcs->scs->use_flat_ipp) {
         if (enc_mode <= ENC_M9) {
             dlf_level       = 6;
-            modulation_mode = 3;
+            modulation_mode = 2;
         } else if (enc_mode <= ENC_M10) {
             dlf_level       = is_not_last_layer ? 6 : 0;
-            modulation_mode = 3;
+            modulation_mode = 2;
         } else {
-            dlf_level       = 0;
-            modulation_mode = 3;
+            dlf_level       = is_not_last_layer ? 6 : 0;
+            modulation_mode = 2;
         }
     } else {
         if (enc_mode <= ENC_M7) {
             dlf_level       = 6;
-            modulation_mode = 3;
+            modulation_mode = 2;
         } else if (enc_mode <= ENC_M9) {
             dlf_level       = is_not_last_layer ? 6 : 0;
-            modulation_mode = 3;
+            modulation_mode = 2;
         } else {
-            dlf_level       = 0;
-            modulation_mode = 3;
+            dlf_level       = is_not_last_layer ? 6 : 0;
+            modulation_mode = 2;
         }
     }
 
@@ -7358,52 +7358,7 @@ void svt_aom_sig_deriv_enc_dec_light_pd1(PictureControlSet* pcs, ModeDecisionCon
     Do not test this signal in M9 and below during preset tuning.  This signal should be kept as an enc_mode check
     instead of and LPD1_LEVEL check to ensure that M9 and below do not use it.
     */
-    if (rtc_tune) {
-        if (pcs->enc_mode <= ENC_M8) {
-            ctx->lpd1_skip_inter_tx_level = 0;
-        } else if (pcs->enc_mode <= ENC_M10) {
-            if (lpd1_level <= LPD1_LVL_2) {
-                ctx->lpd1_skip_inter_tx_level = 0;
-            } else {
-                ctx->lpd1_skip_inter_tx_level = 1;
-                if (((l0_was_skip && l1_was_skip && ref_skip_perc > 35) && me_8x8_cost_variance < (800 * picture_qp) &&
-                     me_64x64_distortion < (800 * picture_qp)) ||
-                    (me_8x8_cost_variance < (100 * picture_qp) && me_64x64_distortion < (100 * picture_qp))) {
-                    ctx->lpd1_skip_inter_tx_level = 2;
-                }
-            }
-        } else {
-            assert(pcs->enc_mode >= ENC_M8 && "Only enable this feature for M10+ in RA or M8+ for low delay");
-            if (lpd1_level <= LPD1_LVL_2) {
-                ctx->lpd1_skip_inter_tx_level = 0;
-            } else if (lpd1_level <= LPD1_LVL_4) {
-                ctx->lpd1_skip_inter_tx_level = 1;
-            } else {
-                ctx->lpd1_skip_inter_tx_level = 1;
-                if (((l0_was_skip && l1_was_skip && ref_skip_perc > 35) && me_8x8_cost_variance < (800 * picture_qp) &&
-                     me_64x64_distortion < (800 * picture_qp)) ||
-                    (me_8x8_cost_variance < (100 * picture_qp) && me_64x64_distortion < (100 * picture_qp))) {
-                    ctx->lpd1_skip_inter_tx_level = 2;
-                }
-            }
-        }
-    } else {
-        if (pcs->enc_mode <= ENC_M9) {
-            ctx->lpd1_skip_inter_tx_level = 0;
-        } else {
-            assert(pcs->enc_mode >= ENC_M10 && "Only enable this feature for M10+ in RA or M8+ for low delay");
-            if (lpd1_level <= LPD1_LVL_2) {
-                ctx->lpd1_skip_inter_tx_level = 0;
-            } else {
-                ctx->lpd1_skip_inter_tx_level = 1;
-                if (((l0_was_skip && l1_was_skip && ref_skip_perc > 35) && me_8x8_cost_variance < (800 * picture_qp) &&
-                     me_64x64_distortion < (800 * picture_qp)) ||
-                    (me_8x8_cost_variance < (100 * picture_qp) && me_64x64_distortion < (100 * picture_qp))) {
-                    ctx->lpd1_skip_inter_tx_level = 2;
-                }
-            }
-        }
-    }
+    ctx->lpd1_skip_inter_tx_level = 0;
     ctx->lpd1_bypass_tx_th = 0;
     if (rtc_tune) {
         if (lpd1_level <= LPD1_LVL_0) {
